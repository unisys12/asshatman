language: php
php:
  - "5.5"
  - "5.4"
  - "5.3"
  - "hhvm"

before_script:
  - composer self-update
  - composer dump-autoload -o
  - sudo mkdir public/css/
  - sudo touch build/css/test1.css

script: phpunit

after_script:
  - sudo rm -rf public/css
  - sudo rm -rf build/css